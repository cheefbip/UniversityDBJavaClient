# UniversityDBJavaClient
Java client for CS 4222's university database project

Running the project:
1. Enter valid database credentials.
2. Run the following code in the root folder on Windows (powershell terminal):
```bash
javac Main.java
java -cp ".;postgresql-42.7.1.jar" Main
```